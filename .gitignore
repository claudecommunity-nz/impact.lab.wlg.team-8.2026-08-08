.venv/
__pycache__/
*.pyc
.DS_Store
.idea/
.env
notebooks/__marimo__/
.playwright-mcp/
*.tsbuildinfo

# --- source data --------------------------------------------------------
# ~66MB of parquet and GeoJSON that belongs to its publishers, not to us.
# Regenerate with `just pull` (needs TRAVEL_INSIGHTS_KEY in .env; ~5 min).
#
# The app does NOT read any of this at runtime — it reads only the derived
# artefacts in team8/poc_1/web/public/data, which ARE committed. What a fresh
# clone loses without a re-pull is the marimo notebook and the ability to
# re-run the pipeline. The demo itself works from the clone.
data/raw/
data/derived/

# --- generated reports --------------------------------------------------
# ~55MB of self-contained profile HTML. Regenerate with `just profile`.
docs/profiles/

# --- scratch ------------------------------------------------------------
/*.png

# Raw screen captures. The 3024x1964 original is ~150MB, over GitHub's 100MB
# hard limit. The compressed cut is committed at docs/demo.mp4 — re-encode with:
#   ffmpeg -i in.mov -vf scale=1920:-2 -c:v libx264 -preset slow -crf 18 \
#          -pix_fmt yuv420p -c:a aac -b:a 160k -movflags +faststart docs/demo.mp4
/*.mov

# Office lock files, created while a deck is open.
~$*
